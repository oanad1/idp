name: Test

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout 
        uses: actions/checkout@v2
      -
        name: Setup node
        uses: actions/setup-node@v2
        working-directory: ./api
        with:
          node-version: "14.x"
      - 
        working-directory: ./api
        run: npm i
      - 
        working-directory: ./api
        run: npm test
